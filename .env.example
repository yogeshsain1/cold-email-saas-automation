# =============================================================================
# Cold Email Automation - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit your .env file to version control!

# =============================================================================
# DATABASE CONFIGURATION (Required)
# =============================================================================
# Turso Database Connection
# Get these from: https://turso.tech/
TURSO_CONNECTION_URL=libsql://your-database-name.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token-here

# =============================================================================
# APPLICATION URLs (Required for production, optional for development)
# =============================================================================
# Public URL of your application (used for unsubscribe links, tracking, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# N8N INTEGRATION (Optional - for advanced workflow automation)
# =============================================================================
# N8N Instance URL (if using n8n for advanced email workflows)
N8N_API_URL=https://your-n8n-instance.com
N8N_API_KEY=your-n8n-api-key-here

# =============================================================================
# NODE ENVIRONMENT
# =============================================================================
NODE_ENV=development

# =============================================================================
# SMTP CONFIGURATION (Configured via UI after deployment)
# =============================================================================
# Note: SMTP settings are configured through the Settings page in the UI
# You can use any SMTP provider:
# - Gmail (smtp.gmail.com:587)
# - SendGrid (smtp.sendgrid.net:587)
# - Mailgun (smtp.mailgun.org:587)
# - AWS SES (email-smtp.region.amazonaws.com:587)
# - Outlook (smtp-mail.outlook.com:587)
# - Custom SMTP server

# =============================================================================
# NOTES FOR JOB APPLICATION USE CASE
# =============================================================================
# 1. Create a Turso account and database at https://turso.tech/
# 2. Copy your connection URL and auth token here
# 3. After deployment, configure SMTP settings in the Settings page
# 4. Create email templates for job applications in the Templates page
# 5. Upload HR contact lists (CSV format) in the Email Lists page
# 6. Create and launch campaigns from the Campaigns page
